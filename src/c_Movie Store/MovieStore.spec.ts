//dont forget to import your functions
import { MovieStore } from "./MovieStore";

describe("Movie Store", () => {
  test("add movies to inventory array", () => {
    const myStore = new MovieStore([], 0);
  });
});
